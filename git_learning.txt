1.提交修改文件：
    git add git_learning.text
    git commit -m “I'm modifying this file”
2.查看工作区的状态：
    git status
3.查看修改前后的差异：
    git diff
4.退回前一个commit版本：
    git reset --hard HEAD^
    前两个版本是HEAD^^,前100个版本是HEAD~100
    ⭐如果你没commit当前的内容，就不要回退，否则就不能回到当前版本！！
5.查看commit的历史记录：
    git log